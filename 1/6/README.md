Exchange emulator.

Prerequisites:
```
pip install -r requirements.txt
```

Build:
```
bash build.sh
```

Run tests:
```
bash run_tests.sh
```


Usage: ```./exchange.out <input_filename> <output_filename> <mode>```

mode: first, last